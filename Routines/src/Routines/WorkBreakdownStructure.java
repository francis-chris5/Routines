
package Routines;

import javafx.scene.control.ScrollPane;


public class WorkBreakdownStructure extends ScrollPane {
    
    /*
    vbox overall with routine at the top (try to integrate other routines eventually -should be able to connect to saved files)
    hbox of tasks just below that: subclass here of text, show name, total cost, start/end dates
    vbox of resources just below each task summary: subclass here of text, show name, role, contact, days off
    */
    
}//end WorkBreakdownStructure
